generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model user {
  pass     String
  reg      String  @id
  branch   String?
  location String?
  name     String?
}
